/**
 * Layer 2 – Workflow Server Layer: creates and saves workflow plans
 * for the factory robots in order to make concrete products.
 * Normally, this layer consists of server components and it can uses
 * services of layer 1 to generate the workflow plans (the instructions
 * that the factory robots will follow to produce products).
 * 
 * @author Tran Xuan Hoang
 */
package layer2;